{{- if .Values.services.consistency.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "brahminykite.fullname" . }}-consistency
  labels:
    {{- include "brahminykite.labels" . | nindent 4 }}
    app.kubernetes.io/component: consistency
  {{- with .Values.services.consistency.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.services.consistency.replicas }}
  selector:
    matchLabels:
      {{- include "brahminykite.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: consistency
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- with .Values.services.consistency.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "brahminykite.labels" . | nindent 8 }}
        app.kubernetes.io/component: consistency
        {{- with .Values.services.consistency.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "brahminykite.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.services.consistency.podSecurityContext | nindent 8 }}
      containers:
        - name: consistency
          securityContext:
            {{- toYaml .Values.services.consistency.securityContext | nindent 12 }}
          image: "{{ .Values.services.consistency.image.repository }}:{{ .Values.services.consistency.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.services.consistency.image.pullPolicy }}
          ports:
            - name: grpc
              containerPort: {{ .Values.services.consistency.service.grpcPort }}
              protocol: TCP
            - name: http
              containerPort: {{ .Values.services.consistency.service.httpPort }}
              protocol: TCP
          livenessProbe:
            grpc:
              port: {{ .Values.services.consistency.service.grpcPort }}
            initialDelaySeconds: {{ .Values.services.consistency.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.services.consistency.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.services.consistency.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.services.consistency.livenessProbe.failureThreshold }}
          readinessProbe:
            grpc:
              port: {{ .Values.services.consistency.service.grpcPort }}
            initialDelaySeconds: {{ .Values.services.consistency.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.services.consistency.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.services.consistency.readinessProbe.timeoutSeconds }}
            successThreshold: {{ .Values.services.consistency.readinessProbe.successThreshold }}
          resources:
            {{- toYaml .Values.services.consistency.resources | nindent 12 }}
          env:
            - name: SERVICE_NAME
              value: consistency
            - name: GRPC_PORT
              value: "{{ .Values.services.consistency.service.grpcPort }}"
            - name: HTTP_PORT
              value: "{{ .Values.services.consistency.service.httpPort }}"
            - name: LOG_LEVEL
              value: "{{ .Values.global.logLevel }}"
            {{- with .Values.services.consistency.env }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          volumeMounts:
            - name: config
              mountPath: /app/config
              readOnly: true
            {{- with .Values.services.consistency.volumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "brahminykite.fullname" . }}-config
        {{- with .Values.services.consistency.volumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.services.consistency.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.services.consistency.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.services.consistency.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}