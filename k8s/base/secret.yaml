apiVersion: v1
kind: Secret
metadata:
  name: brahminykite-secrets
  namespace: brahminykite
  labels:
    app.kubernetes.io/name: brahminykite
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Database credentials
  postgres-password: "brahminykite-prod-2024"
  postgres-user: "brahminykite"
  postgres-db: "brahminykite"
  
  # Redis password (if auth enabled)
  redis-password: ""
  
  # JWT secret for API auth (when implemented)
  jwt-secret-key: "your-secret-key-here-change-in-production"
  
  # Service API keys (for inter-service auth if needed)
  service-api-key: "internal-service-key-2024"